<!-- <script>
	import * as Plot from '@observablehq/plot';
	import * as d3 from 'd3';

	export let path;

	let file = d3.FileAttachment(path).csv({ typed: true });

	Plot.plot({
		marks: [
            Plot.line(file, {
                x: "Time (s) Run 1",
                y: "Vertical Force (N) Run 1"
            })
        ],
	});
</script>

<style>
</style> -->
